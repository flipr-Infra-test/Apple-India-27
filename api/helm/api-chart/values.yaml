replicaCount: 3

image:
  repository: docker-registry.registry.svc.cluster.local:5000/apple-india-27-api
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: api.apple-india-27.flipr.local
      paths:
        - path: /
          pathType: Prefix

rollout:
  canary:
    steps:
      - setWeight: 20
      - pause: {}
      - setWeight: 50
      - pause: {}
      - setWeight: 100

persistence:
  enabled: true
  storageClass: "longhorn"
  size: 5Gi
  mountPath: /app/uploads
